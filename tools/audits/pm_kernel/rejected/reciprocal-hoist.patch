diff --git a/src/oscillators/va/antialias.rs b/src/oscillators/va/antialias.rs
index 8d1bb0b..85023c6 100644
--- a/src/oscillators/va/antialias.rs
+++ b/src/oscillators/va/antialias.rs
@@ -719,6 +719,31 @@ pub(super) fn spline_saw4_narrow(phase: f32x4, phase_step: f32x4, optimized: boo
     phase * f32x4::splat(2.0) - one - correction
 }
 
+// Block-constant support and reciprocal keep division out of PM event handling.
+#[inline]
+pub(super) fn spline_saw4_narrow_precomputed(
+    phase: f32x4,
+    support: f32x4,
+    inverse_step: f32x4,
+    optimized: bool,
+) -> f32x4 {
+    let zero = f32x4::ZERO;
+    let one = f32x4::ONE;
+    let event = phase.cmp_lt(support) | phase.cmp_gt(one - support);
+    let correction = if event.any() {
+        let position = phase.cmp_lt(f32x4::splat(0.5)).blend(phase, phase - one) * inverse_step;
+        let residual = if optimized {
+            optimized_cubic_blep_residual4(position)
+        } else {
+            cubic_blep_residual4(position)
+        };
+        event.blend(residual, zero) * f32x4::splat(2.0)
+    } else {
+        zero
+    };
+    phase * f32x4::splat(2.0) - one - correction
+}
+
 #[inline]
 pub(super) fn spline_saw8_narrow(phase: f32x8, phase_step: f32x8, optimized: bool) -> f32x8 {
     let zero = f32x8::ZERO;
@@ -740,6 +765,31 @@ pub(super) fn spline_saw8_narrow(phase: f32x8, phase_step: f32x8, optimized: boo
     phase * f32x8::splat(2.0) - one - correction
 }
 
+// Block-constant support and reciprocal keep division out of PM event handling.
+#[inline]
+pub(super) fn spline_saw8_narrow_precomputed(
+    phase: f32x8,
+    support: f32x8,
+    inverse_step: f32x8,
+    optimized: bool,
+) -> f32x8 {
+    let zero = f32x8::ZERO;
+    let one = f32x8::ONE;
+    let event = phase.cmp_lt(support) | phase.cmp_gt(one - support);
+    let correction = if event.any() {
+        let position = phase.cmp_lt(f32x8::splat(0.5)).blend(phase, phase - one) * inverse_step;
+        let residual = if optimized {
+            optimized_cubic_blep_residual8(position, event)
+        } else {
+            cubic_blep_residual8(position)
+        };
+        event.blend(residual, zero) * f32x8::splat(2.0)
+    } else {
+        zero
+    };
+    phase * f32x8::splat(2.0) - one - correction
+}
+
 #[inline]
 pub(super) fn spline_blep8_precomputed(
     phase: f32x8,
diff --git a/src/oscillators/va/render.rs b/src/oscillators/va/render.rs
index fea31dc..d631517 100644
--- a/src/oscillators/va/render.rs
+++ b/src/oscillators/va/render.rs
@@ -10,7 +10,8 @@ use super::antialias::{
     bandlimited_pulse4, bandlimited_pulse8, bandlimited_saw, bandlimited_saw_pulse_morph4,
     bandlimited_saw_pulse_morph8, bandlimited_saw4, bandlimited_saw8, bandlimited_triangle,
     bandlimited_triangle4, bandlimited_triangle8, edge_blep, edge_blep4, edge_blep8,
-    spline_blep4_precomputed, spline_blep8_precomputed, spline_saw4_narrow, spline_saw8_narrow,
+    spline_blep4_precomputed, spline_blep8_precomputed, spline_saw4_narrow,
+    spline_saw4_narrow_precomputed, spline_saw8_narrow, spline_saw8_narrow_precomputed,
     spline_triangle4_precomputed, spline_triangle8_precomputed, wrap_phase4, wrap_phase8, wrap01,
 };
 use super::warp::{
@@ -87,6 +88,8 @@ pub fn accumulate_shape8_phase_modulated_block<const SAMPLES: usize>(
     let mut phase = f32x8::from(std::array::from_fn(|index| oscillators[index].phase));
     let phase_steps = f32x8::from(phase_steps);
     let narrow = phase_steps.cmp_lt(f32x8::splat(0.25)).all();
+    let support = phase_steps * f32x8::splat(2.0);
+    let inverse_step = if shape == 2.0 && narrow { f32x8::ONE / phase_steps } else { f32x8::ONE };
     for frame in 0..SAMPLES {
         let current = phase + f32x8::splat(phase_modulation[frame]);
         let current = current
@@ -102,9 +105,10 @@ pub fn accumulate_shape8_phase_modulated_block<const SAMPLES: usize>(
                     Antialiasing::Spline | Antialiasing::SplineOptimized
                 )
             {
-                spline_saw8_narrow(
+                spline_saw8_narrow_precomputed(
                     current,
-                    phase_steps,
+                    support,
+                    inverse_step,
                     antialiasing == Antialiasing::SplineOptimized,
                 )
             } else {
@@ -137,6 +141,8 @@ pub fn accumulate_spline_saw8_phase_modulated_block<const SAMPLES: usize>(
     let mut phase = f32x8::from(std::array::from_fn(|index| oscillators[index].phase));
     let phase_steps = f32x8::from(phase_steps);
     debug_assert!(phase_steps.cmp_lt(f32x8::splat(0.25)).all());
+    let support = phase_steps * f32x8::splat(2.0);
+    let inverse_step = f32x8::ONE / phase_steps;
     for frame in 0..SAMPLES {
         let current = phase + f32x8::splat(phase_modulation[frame]);
         let current = current
@@ -145,7 +151,8 @@ pub fn accumulate_spline_saw8_phase_modulated_block<const SAMPLES: usize>(
         let current = wrap_phase8(current);
         phase += phase_steps;
         phase = phase.cmp_lt(f32x8::ONE).blend(phase, phase - f32x8::ONE);
-        let samples: [f32; 8] = spline_saw8_narrow(current, phase_steps, optimized).into();
+        let samples: [f32; 8] =
+            spline_saw8_narrow_precomputed(current, support, inverse_step, optimized).into();
         for lane in 0..8 {
             output[frame].0 += samples[lane] * left_gains[lane];
             output[frame].1 += samples[lane] * right_gains[lane];
@@ -170,6 +177,8 @@ pub fn accumulate_spline_saw8_phase_modulated_lanes_block<const SAMPLES: usize>(
     let mut phase = f32x8::from(std::array::from_fn(|index| oscillators[index].phase));
     let phase_steps = f32x8::from(phase_steps);
     debug_assert!(phase_steps.cmp_lt(f32x8::splat(0.25)).all());
+    let support = phase_steps * f32x8::splat(2.0);
+    let inverse_step = f32x8::ONE / phase_steps;
     if let Some(phase_modulation) = phase_modulation {
         for frame in 0..SAMPLES {
             let current = phase + f32x8::splat(phase_modulation[frame]);
@@ -179,7 +188,7 @@ pub fn accumulate_spline_saw8_phase_modulated_lanes_block<const SAMPLES: usize>(
             let current = wrap_phase8(current);
             phase += phase_steps;
             phase = phase.cmp_lt(f32x8::ONE).blend(phase, phase - f32x8::ONE);
-            let samples = spline_saw8_narrow(current, phase_steps, optimized);
+            let samples = spline_saw8_narrow_precomputed(current, support, inverse_step, optimized);
             left[frame] = samples.mul_add(left_gain, left[frame]);
             right[frame] = samples.mul_add(right_gain, right[frame]);
         }
@@ -188,7 +197,7 @@ pub fn accumulate_spline_saw8_phase_modulated_lanes_block<const SAMPLES: usize>(
             let current = phase;
             phase += phase_steps;
             phase = phase.cmp_lt(f32x8::ONE).blend(phase, phase - f32x8::ONE);
-            let samples = spline_saw8_narrow(current, phase_steps, optimized);
+            let samples = spline_saw8_narrow_precomputed(current, support, inverse_step, optimized);
             left[frame] = samples.mul_add(left_gain, left[frame]);
             right[frame] = samples.mul_add(right_gain, right[frame]);
         }
@@ -211,6 +220,8 @@ pub fn accumulate_spline_saw4_phase_modulated_block<const SAMPLES: usize>(
     let mut phase = f32x4::from(std::array::from_fn(|index| oscillators[index].phase));
     let phase_steps = f32x4::from(phase_steps);
     debug_assert!(phase_steps.cmp_lt(f32x4::splat(0.25)).all());
+    let support = phase_steps * f32x4::splat(2.0);
+    let inverse_step = f32x4::ONE / phase_steps;
     for frame in 0..SAMPLES {
         let current = phase + f32x4::splat(phase_modulation[frame]);
         let current = current
@@ -219,7 +230,8 @@ pub fn accumulate_spline_saw4_phase_modulated_block<const SAMPLES: usize>(
         let current = wrap_phase4(current);
         phase += phase_steps;
         phase = phase.cmp_lt(f32x4::ONE).blend(phase, phase - f32x4::ONE);
-        let samples: [f32; 4] = spline_saw4_narrow(current, phase_steps, optimized).into();
+        let samples: [f32; 4] =
+            spline_saw4_narrow_precomputed(current, support, inverse_step, optimized).into();
         for lane in 0..4 {
             output[frame].0 += samples[lane] * left_gains[lane];
             output[frame].1 += samples[lane] * right_gains[lane];
